export type Error = {
    message: string;
    // Add any other properties that your errors might have
  }

export type CustomError = {
    message: string;
    // Add any other properties that your errors might have
  }